---
description: 将 CLAUDE.md 的编码准则作为本项目默认规则
---

你在本项目中工作时，必须遵循 `CLAUDE.md` 的全部要求（位于项目根目录 `CLAUDE.md`）。

重点约束（不取代原文，仅作为提醒）：
- 先思考再编码：不做无根据假设；不确定就说明不确定；给出权衡；必要时先澄清。
- 简单优先：只做用户要求的最小改动，不做“顺手优化/扩展/可配置化”。
- 外科手术式修改：只改必须改的行；不要重构无关代码/格式；只清理由你改动造成的无用代码。
- 目标驱动并可验证：将任务拆成可验证的成功标准；能跑测试/检查就跑并确认通过。
